from django.apps import AppConfig

class MisaelLinkConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name               = 'misael_link'
    verbose_name       = 'Conexión Centro Misael'
